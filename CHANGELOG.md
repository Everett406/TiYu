# 更新日志 (Changelog)

本文件记录题屿（TiYu）每个版本的变更明细。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本（MAJOR.MINOR.PATCH）。

## [Unreleased]

## [2.10.3] - 2026-09-05

### 修复 —— 切题库/换题型范围后刷题进度被清零（第三十九轮：用户真机反馈）

- **症状**：切换题库（有时又没事）再回来，刷题只能从第 1 题重开、
  「接续上次进度」消失；但首页已刷/正确率/连击等数据都还在。
- **根因**：会话快照旧版只有顺序/随机两个存储槽，切库开刷（哪怕只进页
  没答题）或换题型范围就**覆盖销毁**原快照；作答记录在 DB 不受影响，
  故「进度没了、统计还在」。v2.8.8 只堵了读口（跨库不误显），未堵写口覆盖。
- **修复**：快照升级多槽存储（题库 × 模式 × 题型范围各自占槽，LRU 上限 10）：
  切库/换范围互不覆盖，切回原样接续；「继续刷题」接该题库+模式最近一份；
  旧进度一次性无损迁移；「重新开始」只清当前槽；删库清该库会话；
  题库升级/清空数据仍整体作废（旧题 id 失配，预期）。
- 恢复跳页改为恢复时捕获 index（去掉跳页时对 DataStore 的二次读）。
- static_check：多槽 API 必备断言 + 旧单槽写法残留即 FAIL；
  修正 widget 布局禁令检查路径（历史拼错路径致该组检查空转，本次起真正生效）。

## [2.10.2] - 2026-09-05

### 修复 —— 桌面小组件 ColorOS 兼容性（第三十八轮：真机反馈）

- **「载入窗口小部件时出现问题」**（ColorOS / 一加等机型四款全触发）：
  四款小组件布局整体收敛到 RemoteViews 最保守兼容子集——
  - 学习统计卡进度环由 layer-list(rotate+ring shape) 矢量改为**预渲染
    PNG**（0~100% 每 10% 一档 × 深浅两套共 22 张，PIL 超采样抗锯齿，
    270° 起笔 butt 端头与原矢量一致），布局改用 ImageView；运行时
    `setImageViewResource` 切档，不再依赖 ProgressBar/theme attr/rotate；
  - 移除 ProgressBar `style="?android:attr/progressBarStyleHorizontal"`
    主题属性引用（宿主 Context 无 app theme 时 inflate 直接抛异常）；
  - 移除全部 `android:letterSpacing`（OEM 宿主 inflater 历史坑）。
- static_check 新增小组件保守化禁令（禁 theme attr / letterSpacing /
  ProgressBar 进 widget 布局；必须含 PNG 档位；旧矢量环必须删除）。

## [2.10.1] - 2026-09-05

### 修复 —— 成绩卡体验（第三十七轮：用户真机反馈）

- **成绩卡预览边界**：预览加投影（16dp）与 1dp 细描边，深色主题
  （墨黑/深色金辉）下卡片轮廓与页面背景一眼可辨；卡片与下方
  「主题」区间距 18→28dp，层次更清晰；占位态（正在生成）同款处理。
- **署名 / 标语输入框**：修复占位提示在真机上不显示的问题——
  原 decorationBox 将提示 Text 与内容槽裸顺序排列（无叠加容器），
  改为标准 Box 叠加写法；补 `cursorBrush` 光标色（ui.ink）；
  输入框背景 alpha 0.05→0.07、边框 0.12→0.22，对比度提高。

### 调整

- **移除设置页「每日目标」滑杆**（v2.10.0 引入）。底层 dailyGoal
  键保留：统计卡进度环与小组件目标按默认 30 题计算，行为不变。

## [2.10.0] - 2026-09-05

### 新增 —— 桌面小组件 + 长按快捷方式 + 成绩分享卡（第三十轮需求）

#### 桌面小组件（util/WidgetUpdater.kt，RemoteViews 零新依赖）

- 四款极简杂志风小组件：纯白/墨黑底 + 超大数字 + 细线分隔，深浅色程序化
  自适应（不依赖 values-night，RemoteViews 下更可靠）；
- **继续刷题磁贴**（2×2）：今日题量大数字 + 连续打卡天数，点按直落上次进度；
- **学习统计卡**（4×2）：每日目标进度环（ring shape useLevel 弧）+ 正确率 +
  连续打卡 +「距今日目标还差 N 题」（达标变绿）；
- **错题特训卡**（2×2）：待攻克错题大数字（>0 红 / =0 绿），点按直落特训；
- **快速模考贴**（2×2）：上次模考成绩/合格态/题数，点按沿用上次配置再考一场；
- 更新触发：recordAnswer / submitExam / abandonExam 钩子（600ms 节流合并）
  + updatePeriodMillis 30 分钟兜底；设置-刷题新增「每日目标」滑杆（5–200）。

#### 长按图标快捷方式（res/xml/shortcuts.xml + LauncherBus）

- 四项：**继续刷题**（接续上次会话）/ **错题特训**（沿用上次筛选，
  WrongBookScreen 开特训时记忆）/ **随机小练**（随机抽 20 题立即开练）/
  **快速模考**（沿用上次组卷配置直接开考，无配置回模考配置页兜底）；
- MainActivity 改 singleTask，冷启 onCreate / 热启 onNewIntent 统一写
  LauncherBus（mutableStateOf），AppRoot snapshotFlow 消费后路由；
- 随机小练走 src="random" 新分支：不恢复/不补漏/不落盘快照（不污染双槽）。

#### 成绩分享卡（util/ShareCardRenderer.kt + ui/share/ShareCardSheet.kt）

- 模考成绩页新增「生成成绩卡」，全屏浮层预览 + 个性化 + 保存/分享；
- Canvas 1080×1440 渲染（预览即最终产物，双实现零漂移），内置字体
  Noto Sans/Serif SC + 霞鹜文楷，零第三方依赖；
- 7 套主题：日落 / 极光 / 薄荷（渐变）、液态玻璃（柔光斑+细描边+文楷）、
  纸感白 / 墨黑（衬线大字）、深色金辉（双金线边框）；
- 内容模块：大分数 + 合格徽章 + 正确率/用时/答对/连击四宫格 +
  「较上次 ↑N 分」同库对比 + 累计刷题/打卡行 + 品牌页脚；
- 个性化全套自定义：署名（≤8 字）、标语（≤30 字）、强调色微调
  （默认/绯红/靛蓝/鎏金/松绿），即改即存 DataStore 下次记住；
- 保存 = MediaStore Pictures/题屿（minSdk 31 免权限，GallerySave.savePngBitmap）；
  分享 = FileProvider + ACTION_SEND（GalleryShare.sharePngImage，v2.8.2 基建复用）。

### 变更

- MainActivity → launchMode="singleTask"（快捷方式/小组件热启动走 onNewIntent）；
- practiceRun 路由新增可选 limit 参数（带 defaultValue，老调用点零改动）；
- Repo 新增 examRecord(examId) 访问器（成绩卡取用时/开考日期）；
- static_check must 断言 +6（WidgetUpdater/ShareCardRenderer/ShareCardHost/
  ExamQuickConfig/LauncherBus/savePngBitmap）。

## [2.9.3] - 2026-09-05

### 新增 —— 关玻璃分支整体改为果冻（Gooey）模式（第二十九轮需求）

- **三级特效体系**：液态玻璃（mode 0，真折射渲染路径零改动）→
  果冻（mode 1）→ 安全平涂（mode 2，崩溃无条件兜底，独立于用户设置）；
  `GlassRuntime` 由单一布尔改为三态 `mode`，`enabled` 语义保持兼容
  （= mode 0），切换即时生效无需重启；
- **亚克力表面**（`ui/glass/Acrylic.kt`）：沿用 Kyant0 backdrop 管线但只留
  模糊（默认 8dp，参数化）、去掉 vibrancy / lens，配柔和阴影 +
  0.75dp 细描边；亮色 White α≈0.6 / 暗色主题色 α≈0.45；
- **gooey 动效层**（`ui/gooey/Gooey.kt`）：官方
  `RenderEffect.createChainEffect(阈值, blur)` 管线——层内容先高斯模糊、
  再 alpha 阈值切割，元素靠近处形成 metaball 液态桥接；API 33+ 用
  AGSL `step` 阶跃（RuntimeShader），API 31–32 用 `ColorMatrixColorFilter`
  alpha 行高对比斜坡近似；构建异常 runCatching 回退无特效，绝不崩溃；
- **动效接入点**（仅果冻模式生效）：底栏选中胶囊快 spring + 拖尾胶囊慢
  bounce 的液态融合（sinasamaki metaball 手感）；Toggle 滑块拉伸融合；
  Slider 拇指速度拖尾；GlassButton 按压液泡鼓包（表面 blob 进 goo 层、
  文字图标在外保持锐利）；弹窗/面板过冲缩放入场（缩放融合）；多选选项
  勾选框与对勾液态融合弹出、框体脉动一次回弹；
- **文字安全**：文字/长文本/列表内容零接触 goo 阈值层（goo 层只含
  表面液滴与动效液滴）；
- **设置**：特效文案改「液态玻璃 / 果冻」二选一（不加第三项）；
  glassBlur 三档在果冻模式映射为亚克力模糊（6/8/12dp）与 goo 强度
  （threshold 0.56/0.50/0.44）；
- **减弱动画**：系统「减弱动画」（ANIMATOR_DURATION_SCALE=0）时只去
  果冻动效、保留亚克力表面（`rememberReducedMotion`）；
- **防误删**：static_check 新增 6 条 gooey must 断言。

### 工程 —— 独立分支开发与开玻璃回归核验

- 功能在 feature/gooey-mode 分支开发（提交 edefd3f）后 merge 回 main；
- 「开玻璃」回归核验：git diff 逐文件确认 `GlassRuntime.enabled` 渲染
  路径逐行未动——glass() 玻璃分支 / GlassCard / GlassButton /
  GlassSlider / GlassToggle 玻璃分支 / GlassBottomBar 玻璃分支均原样；
  弹窗动画在玻璃/安全模式下参数逐值等价（仅果冻模式换过冲手感）。

## [2.9.2] - 2026-09-05

### 修复 —— 无分类题的分类标签回退显示题库名（第二十八轮反馈）

- **现象**：自定义题库导入的题没填分类时，刷题题目卡片上的分类标签恒为
  「未分类」，看不出题目来自哪个题库；
- **根因**：导入时空分类落库为字面量「未分类」（`Repo` 的 `ifBlank` 兜底 /
  CSV 无分类列默认值），各展示点直读该字段原样渲染；
- **修复**：新增 `displayCategory` 展示回退——分类为空/为「未分类」时显示
  题目所在题库的名称（`rememberBankName` 按 `q.bankId` 查询，题库改名自动
  跟随，切题库即时刷新）；四处展示点统一接入：刷题题目卡 / 模考题目卡 /
  搜索结果 / 错题本条目；内置题库有真实分类，不受影响。

### 修复 —— 模考成绩页错题解析展开无动画（第二十八轮反馈）

- 成绩页「错题解析（N）」卡片此前直出直隐；补上与搜索结果展开同款的
  弹性 `expandVertically/shrinkVertically`（spring 0.85/380）+ 渐隐渐显。

### 调整 —— 打赏门槛计时口径对齐文案（第二十八轮核实）

- 用户核实：「使用本题库 2 小时」是否指刷题 2 小时——此前计时器统计的是
  App 全程前台时间（每分钟 +1），与弹窗文案「累计刷题超过 2 小时」不符；
- 新增 `UsageSignals.onPracticeScreen`（`PracticeRunScreen` 组合期置位 /
  离开复位），`MainActivity` 心跳计时按此门控：只有真实停在刷题答题页才累计；
  浏览其他页面、后台、模考均不计时，`onStop` 仍兜底取消心跳。

## [2.9.1] - 2026-09-04

### 优化 —— 首启引导 Hero 式转场 + 文案精简（第二十七轮反馈）

- **步骤切换 Hero 化**：高亮挖孔不再「消失-重现」，而是从上一步位置平滑飞向
  下一步（430ms FastOutSlowIn 插值，呼吸描边全程跟随）；玻璃气泡与挖孔同节奏
  飞行，落位仍是「下方优先、不足放上方、夹边防出屏」；锚点缺席时挖孔原地淡出、
  恢复时原地淡入，不再瞬间跳变；
- **气泡内容方向感知切换**：标题/正文随行进方向（前进上滑 / 后退下滑）滑动 +
  渐隐渐显（AnimatedContent），卡片高度随内容平滑过渡；步骤圆点当前步加宽
  改为渐变动画（animateDpAsState）；
- **开场/收尾动画**：整个引导层随开始淡入、随结束/跳过淡出（此前为瞬间出现/
  瞬间消失）；
- **文案精简**：8 步正文全部压缩为一句话（欢迎卡两行），去掉功能细节罗列，
  每步一眼看完；
- **修复**：欢迎卡 CTA 此前被固定渲染为「下一步」（step.cta 从未生效），
  恢复设计文案「开始逛一逛」；
- **加固**：引导层此前实际未接管触控（Canvas 不参与命中测试，转场窗口期
  可误触下层界面）——overlay 现挂 pointerInput 成为命中目标并在 Final 段
  消费事件，引导期间 App 本体彻底只读，气泡内按钮（Main 段判定）不受影响；
- 实现说明：动画统一改用 `Animatable`（顶层 `animate` 构建器在本版本解析到
  internal 重载，弃用）；挖孔/气泡绘制位置由 from/to/进度推导，转场中目标
  变化时自动从当前位置接力飞行。

## [2.9.0] - 2026-09-04

### 新功能 —— 首启功能引导（第二十六轮反馈，用户口径：页面高亮气泡式）

- 首次启动自动弹出「欢迎来到题屿」引导卡，随后 7 步高亮气泡导览：
  首页总览 → 刷题页搜索框 → 开始刷题 → 模考 → 错题本 → 设置·题库导入 → 设置·个性化；
- **高亮交互**：遮罩整屏变暗，目标元素挖圆角孔 + 呼吸描边（accent 柔光外圈），
  玻璃气泡（与弹窗同款折射材质）在锚点下方/上方自适应落位，滚动中实时跟随；
- **锚点自动滚进屏幕**：切到对应 tab 等转场后 bringIntoView 把目标滚入视口再放气泡；
- **锚点缺失兑底**（如空错题本没有「开始特训」按钮）：整屏变暗 + 气泡居中，不空转；
- 「跳过引导」随时可点（或系统返回键），跳过/翻完都落 `onboarding_done` 标记不再自动弹；
- 设置页新增「使用引导」入口（题库管理与关于之间，用户指定位置），可随时重看；
- 引导期间 App 本体只读（遮罩吃掉全部触控），切页由引导驱动，状态不脱钩；
- 新增 `ui/onboarding/Onboarding.kt`（Tour / OnboardingBus / OnboardingAnchors / TourHost）、
  `SettingsStore.onboarding_done`、6 屏 7 锚点埋点；
- 兼容性适配：项目里 ui 被抬到 1.9.2——`Modifier.composed` 已隐藏、
  `androidx.compose.ui.BringIntoViewRequester` 已移除，改用 foundation 1.8.3 的
  `relocation.BringIntoViewRequester`（桥接新节点 API，功能等价）。

## [2.8.10] - 2026-09-04

### 修复 —— 关于弹窗内点「请喝奶茶」，打赏弹窗被压在下面（第二十五轮反馈）

- **现象**：关于弹窗开着时点「☕ 请喝奶茶」，打赏弹窗出现在关于弹窗**下方**，
  内容被遮挡且无法正常点按；
- **根因**：打赏弹窗宿主（`SupportPromptHost`）在 App 启动时即注册进 `GlassOverlayPortal`
  （entries 列表底部），关于弹窗在进入设置页时才注册（列表顶部，「后组合者在上」）——
  同屏时打赏层永远在关于层之下，且关于层的透明点击层会拦截打赏面板的触控；
- **修复**：交互改为两步衔接——点「☕ 请喝奶茶」后关于弹窗先自己关闭，
  待退场动画视觉完成（420ms）再置 `SupportBus.manualOpen` 拉起打赏弹窗，
  两者不再同屏，层级问题根治；
- 手动打开不占自动触达机会的语义不变（`manual` 路径仍不标记 `support_prompted`）。

## [2.8.9] - 2026-09-04

### 关于 —— 页面弹窗化（第二十四轮反馈）

- 设置 → 关于由内联卡片改为**单行入口**（图标 + 「关于题屿 · vX.Y.Z 版本与支持」+ 箭头），
  点开**从底部弹出**玻璃弹窗（`GlassBottomSheet`，带把手、下滑跟手关闭、上拉橡皮筋）；
- 弹窗为文档式排版（参考作者旧版刷题本）：**文楷**标题「关于题屿」+ 虚线分隔 +
  三段应用介绍正文（均为应用内置文楷字体，不随阅读字体设置变化）；
- 版本行（`vX.Y.Z · GitHub 发布页`）可点直达 Releases（沿用 v2.8.8 行为）；
- 「☕ 请喝奶茶」与「检查更新」双胶囊收纳进弹窗：奶茶入口复用打赏弹窗
  （叠加打开不占自动触达机会），检查更新走原逻辑，结果行显示在按钮正下方。

### 修复 —— 检查更新提示串位

- 「已是最新版本 / 检查失败」此前写入题库管理区的 `importMsg` 状态，
  导致提示出现在**题库管理卡片**而非关于区（用户实测复现）；
- 新增独立 `updateMsg` 状态，只在关于弹窗内、检查更新按钮下方渲染，
  并从 `importMsg` 的成功色前缀表中移除「已是最新版本」。

### 修复 —— 「请作者喝杯奶茶」换行不等高

- 原胶囊文本过长，小屏上换行成两行、与右侧「检查更新」高度不齐；
- 双按钮改用 `GlassButton` 固定 44dp 高 + `maxLines = 1`，文案精简为「请喝奶茶」，
  彻底杜绝换行；检查更新按钮补刷新图标。

## [2.8.8] - 2026-09-04

### 刷题 —— 切库数据混乱修复（第二十三轮反馈，P0）

- **根因**：刷题会话快照（顺序/随机双槽）虽带 `bankId`，但配置页续刷提示行未校验题库归属，
  「将接续上次进度 · 第 523 / 2000 题」显示的是异库旧进度；切换随机又露出内置题库的 20 题旧会话；
- **修复**：`SettingsStore.practiceSession()` 在唯一读口按 `currentBank` 过滤
  （快照 bankId 与当前题库不一致即视同无会话），配置页提示、刷题页恢复、自动接续、
  补漏轮全链路一次堵住；配置页 `snapReusable` 再加一道显式校验双保险；
- **语义**：进度按题库各自保存、互不干扰——切回旧题库时原进度仍在，不丢数据；
- 顺带消除异库旧会话引发的整库重复加载（2000 题 IN 查询 / 3059 题全量取回）。

### 流畅度 —— 两处重组/加载浪费

- `RootSettings` 剔除 `usageMs/supportPrompted/supportRefused`：前台计时每分钟 +60s
  都会重发 settings flow，此前挂在根级导致「MainActivity → AppRoot → NavHost」
  每分钟全量连锁重组（滚动中偶发掉帧）；打赏门槛判定下沉到 `SupportPromptHost`
  内部自行收集，重组范围收敛到该小组件；
- 异库旧会话的重复加载浪费（见上）一并消除。

### 关于 —— 检查更新（引导 GitHub Releases）

- 设置 → 关于，新增**「检查更新」**胶囊：调 GitHub API `releases/latest` 比对版本号
  （数字段逐段比较，"2.8.10" > "2.8.9"）；有新版弹玻璃对话框**引导浏览器打开发布页**
  自行下载（明确不做自动下载/静默安装，用户口径）；无新版轻提示「已是最新版本」，
  网络失败提示「检查失败，请稍后重试」；
- **版本行可点**：直达 GitHub Releases 页；
- 新增 `util/AppUpdater`（HttpURLConnection + org.json，零新依赖）；
- Manifest 新增 `INTERNET` 权限（仅检查更新用，题库功能仍完全离线）。

### 仓库 —— 更名 TiYu

- GitHub 仓库 `Everett406/DroneQuiz` 更名为 **`Everett406/TiYu`**（题屿拼音；
  GitHub 仓库名不支持纯中文，「题屿」会被消毒为 "-"）；描述同步为「题屿 TiYu — …」；
- 旧链接（含 Releases/Issues）GitHub 自动 301 重定向，**不会 404**；
- 安装包更名 `TiYu-x.y.z.apk`、Gradle 项目名 `TiYu`、README 徽章/链接/CI 监控地址全部同步；
- 包名保持 `com.drone.quiz` 不变（改动会导致覆盖安装变全新应用、数据丢失）。

## [2.8.7] - 2026-09-04

### 设置 —— 防沉迷更名 + 说明字全面压缩（第二十二轮反馈）

- **「护眼提醒」更名为「防沉迷」**（用户口径）；说明字同步压缩：
  「已开启 · 20 分钟提醒」/「刷题 20 分钟提醒休息」；
- **全部设置项说明字统一压缩至 ≤10 字左右（硬上限 15，不换行）**：
  画面特效（"玻璃折射 · 推荐"/"已关闭 · 省电"）、及格分（"80 分（50–95）"）、
  全局壁纸（"已设置 · 全局背景"，28 字长说明段删除）、
  每日提醒（"已开启 · 刷过不打扰"/"每天定时提醒"）、题库管理脚注重写。

### 题库 —— 重命名

- 题库管理每行新增**铅笔入口**（新增 `AppIcons.Edit` 填充铅笔图标），
  点击弹出**玻璃输入对话框**（新增 `GlassInputDialog`：单行输入、空名守卫、限长 16 字）；
- 数据链路：`BankDao.rename`（UPDATE 语句）+ `Repo.renameBank`（trim/限长/空名拒绝）；
- 内置/导入题库均可重命名——name 仅 banks 表展示字段，与播种版本/删除墓碑机制无关。

### 错题本 —— 删除体验

- **删除按钮**：裸 16dp 图标 → **30dp 触达区**，并左移避开右侧快速滚动把手轨道
  （原 44dp 宽轨道横踞整个右缘，删除点击会被把手手势吞掉——"挨得太近不好点"的共犯）；
- **把手轨道收窄** 44→24dp；
- **删除动画两段式**：点击先缩放淡出（spring 缩至 0.82 + 170ms 淡出，图标同步变红），
  150ms 后落库触发 `Modifier.animateItem` 位置动画，其余条目**弹性靠拢补位**。

### 小字 —— 自适应背景

- 新增 `readableSubColor()`：复用 v2.8.4 `backdropIsDark()`（壁纸亮度 × 主题纱合成判定），
  壁纸明暗与主题**同向**保持原 textSub 观感；**翻暗**提亮为暖沙色、**翻亮**压深为暖灰；
- 接入范围（全部直接坐在壁纸上、被用户圈出"看不清"的文字）：
  `SectionLabel` 全部分区标题（题目范围/题目顺序/学习概览/全局壁纸等）、
  `ScreenTitle` 页面副标题（"外观 / 刷题 / 数据""选择范围，开始你的练习"等）、
  刷题页"将接续上次进度"页脚、错题本列表脚注、首页"每日精进"胶囊。

## [2.8.6] - 2026-09-04

### 提醒 —— 护眼提醒（第二十一轮反馈，防沉迷口径）

- **设置 → 提醒新增「护眼提醒」开关**（`eyeCareReminder`，默认关闭）；
- **连续刷题满 20 分钟弹窗提醒**：刷题页前台累计计时（每 15s 校验且仅在 RESUMED 时累计，
  切后台自动暂停，退出刷题页计时即止），弹液态玻璃对话框提示「看看 6 米外远处 20 秒」
  （20-20-20 护眼法则），关闭后重新计时、每满 20 分钟可再次触发；
- 考试/模考完全不受影响（计时器只存在于刷题页）。

### 刷题 —— 顺序刷题循环补漏

- **「刷到末题」即本轮结束**（`sessionAtEnd`，替代原「全部答完」口径）：
  会话刷到最后一题后，下次进入不再停留在末题，而是自动开新一轮；
- **只挑没刷过的题**（`loadCatchUpRound`）：会话快照新增 `covered` 字段（本轮周期内
  已覆盖题目累计，随快照落盘、补漏轮链式继承），新一轮题单 = 总题单 − covered − 上轮 answers。
  例：直接跳到第 200 题往后刷完 → 下轮自动从第 1 题补刷没碰过的 199 题；
- 补漏轮也刷完（全覆盖）后 → 开完整新一轮；仅顺序模式 + 纯刷题（src=all）+ 筛选参数
  与上轮一致时适用，随机模式与错题特训不受影响；配置页「将接续上次进度」提示同步新口径。

### 模考 —— 成绩单回显合格线

- **数据库 v4**（`MIGRATION_3_4`）：exam_records 新增 `passLine` 列
  （`@ColumnInfo(defaultValue = "60")` + ADD COLUMN DEFAULT 对齐，v2.8.1 教训延续）；
- 开考时把当时设定的及格分定格写入记录，成绩单显示「答对 X / Y 题 · 合格线 N 分」；
  历史记录回显默认 60（旧记录未存档，及格判定仍以存档的 passed 为准，不受影响）。

### 导入 —— 界面清理

- 删除「导入 CSV / 导入 ZIP」卡片内的灰色描述小字（"Excel / AI 生成的表格"
  "CSV + 图片，支持带图题目"）与底部说明段落（用户口径：界面干净）；
- 底部按钮简化命名：「分享 CSV 模板」→「CSV 模板」、「复制 Agent 提示词」→「提示词」；
  提示词弹窗 hint 措辞统一为「Agent」（不再出现 "AI Agent"）。

### 图片 —— 小图更小

- 题目图片小图限高 96–150dp → **64–96dp**，且**宽度贴合图片等比宽**（不再整行铺满），
  展开时宽度同步过渡到整卡（宽高双轴 spring 插值，Hero 感更强）；
- 删除「点按放大 / 点按收起」角标（用户口径：不需要写出来）。

### 修复 —— 收款码圆角裁切

- 打赏页收款码截图素材本身为直角，未被外层圆角裁切导致圆角白卡内露出直角图片边缘；
  现给图片自身加 10dp 内层圆角，与外层 18dp 白卡同心。

### 性能 —— 大题库卡顿治理

- **根因**：DataStore 对任意 key 写入都会重发完整 settings——刷题每次翻页/作答都会落盘
  会话快照，此前 MainActivity 根组合整体收集 settings，导致每次快照写入都触发
  「根 → AppRoot → 整个 NavHost」连锁重组，叠加液态玻璃渲染产生可感知卡顿
  （导入大题库后翻页更频繁、感知更明显）；
- **收敛**（`RootSettings` + `conflateForRoot`）：根组合只订阅主题/字体/特效/壁纸/打赏门槛
  字段并结构相等去重；刷题页同样改为只订阅 autoNext / removeThreshold / 护眼开关
  三个字段；无关写入不再穿透到根；
- **题单上限 800 → 2000**：修复导入大题库（驾考 1000+ 题）后超出部分永远进不了题单的问题。

## [2.8.5] - 2026-09-04

### 导入 —— ZIP 图片题库（第二十轮反馈）

- **导入方式重构为「CSV 表格 / ZIP 压缩包」双通道**：JSON 导入下线（`parse()` 对 JSON 头给出友好迁移提示；
  内置题库播种仍走 JSON 资产，不受影响）；
- **ZIP 导入**（`BankImport.parseZip`）：ZIP 内有且仅有一个题目 CSV（任意层级），
  图片文件（jpg/jpeg/png/webp/gif/bmp）允许在压缩包任意层级，按文件名（忽略大小写）匹配；
  兼容只打包单个 CSV 的 ZIP（等同纯 CSV 导入）；
- **CSV 新增「图片」列**（紧跟题干，兼容「配图/插图/image/images/picture」表头）：
  填图片文件名，多图用 `|` 分隔；纯 CSV 导入填了图片会逐行报错并提示改用 ZIP；
  引用了压缩包中不存在的文件名会明确报错；
- **图片随题库落盘**（`QuestionImages`）：`filesDir/bank_images/<bankId>/`，仅保存被题目引用的图片，
  文件名清洗（去路径/非法字符）+ 重名自动加序号；删除题库时一并清理图片目录；
- **数据库 v3**（`MIGRATION_2_3`）：questions 表重建式加 `images` 列（JSON 文件名数组），
  非空新列禁用 ADD COLUMN DEFAULT（v2.8.1 教训延续）；
- **导入预览补充图片信息**：显示「图片 N 张 · 带图题目 M 题」。

### 导入 —— 复制 Agent 提示词

- **「查看 CSV 模板」升级为「复制 Agent 提示词」**（`GlassPromptDialog`）：
  弹窗内可滚动预览完整提示词（输出形式二选一：纯文字 → CSV；带图 → ZIP + images 文件夹；
  列格式、各题型答案写法、图片规则、硬性要求），一键复制；
- 用户把提示词连同自己的 Excel / Word / PDF 材料交给 AI Agent，由 Agent 整理成题屿可导入的
  CSV / ZIP，拿到文件直接导入；
- **CSV 模板同步更新**：新增「图片」列 + 第 6 行示例（讲解带图题库如何组织 ZIP 导入）；
  分享入口文案改为「分享 CSV 模板」；导入弹窗顶部说明小字删除（用户反馈），ZIP/CSV 选择卡自带说明。

### 刷题 —— 题目图片展示

- **带图题目渲染**（`QuestionImageStrip`）：刷题页与模考页题卡在题干后渲染图片，
  填空题图片位于题干输入区上方（同一插入点，不产生重复题干）；
- **小图点按展开**：默认限高小图（96–150dp，按图片宽高比自适应），点按平滑放大至大图
  （最高 440dp，spring 插值高度与圆角，Hero 式过渡），再点收起；
- 图片按 `QuestionImages.load` 降采样解码（最长边 1440px）+ 32MB LruCache，
  加载失败降级为「图片未能加载」占位，不阻塞答题。

## [2.8.4] - 2026-09-04

### 模考 —— 组卷题型修复（第十九轮反馈，最高优先）

- **修复「20 题的库无论怎么调只出 4 题」**：`examTypeOrder` 是全局持久化的排序提示，此前被当成白名单
  （`typeOrder.filter { it in bankTypes }`），在无人机库（单选+判断）保存过的顺序会把示例库的多选/填空/简答整体过滤掉，
  示例库 single 2 + judge 2 → 无论如何调整最终只出 4 题。改为 `buildList`：先按已存顺序列出的题型，再把库内其余题型按规范序追加，永不丢题型
- **同根因一并修复**：题型构成关闭自动后只剩判断和单选、题型顺序奇奇怪怪
- **滑杆位置联动**：GlassSlider 同步循环此前捕获首帧 value lambda（捕获局部 `val r`），
  外部改值后 snapshotFlow 永远读旧值 → 联动滑杆只变数值不动位置；改 `rememberUpdatedState` + 调用侧读委托状态，双保险
- **高级选项不再自动收起**：根因是 GlassToggle 拖拽手势不消费 down/up，卡内开关点击会双触发整卡 onClick 翻转 `showAdvanced`；
  onClick 从整卡移到标题行，卡内控件不再影响面板开合
- **第 4 张小卡补齐**：非经典模式考试设置第二行原为「及格分 + 空位」3 卡不对称；
  新增「题型构成」卡（自动/手动配比状态 + `MixPreviewBar` 各题型占比分段预览条），点击直达高级选项，2×2 网格恢复对称

### 刷题 —— 题型多选（第十九轮反馈）

- 「全部题型」筛选保留「全部」，其余题型 chips 改**多选**：空集=全部，勾选任意组合（如「单选+判断」），全选自动归位「全部」
- 编码：开始刷题时按规范序合成逗号串（`single,judge`）经导航参数（Uri.encode）传入，
  `splitTypeFilter` 解析；DAO 新增 `idsByFilterTypes`（type IN 查询），Repo `loadPractice` 增 `types` 参数；
  会话快照 type 字段存同串，字符串相等比对，无迁移
- 错题特训多题型兜底：无多题型 DAO，先取活跃错题再内存过滤

### 刷题 —— 填空题视觉重做（第十九轮反馈）

- **空位重设计**：宽度自适应内容（空态最小 60dp、随输入增长、上限 170dp；原 110–220dp 固定宽），
  字号 15→14sp、内边距收窄、圆角 10→9dp、占位「第N空」缩小居中；FlowRow 间距收紧
- **题目重复修复**：刷题页与模考页题卡此前先渲染整段 `q.text`，填空题又在 `BlankInlineFields` 拆段重渲染 → 题目显示两份；
  填空题型下不再单独渲染题干

### 提交按钮自适应壁纸（第十九轮反馈）

- 新增 `LocalWallpaperLuminance`（AppRoot 加载壁纸时 IO 线程降采样 12×12 求平均亮度）与 `backdropIsDark()`
  （壁纸亮度 × 主题纱 alpha 合成估算实际背景明暗）
- `SubmitAnswerButton`：背景明暗与主题相反时（如亮色主题 + 深色壁纸）墨色/奶色底字自动反色；
  禁用态文字按背景明暗选色（深背景亮灰字），任何壁纸下清晰可读

## [2.8.3] - 2026-09-04

### 模考 —— 题型构成重构（第十八轮用户反馈）

- **题型构成移入高级选项**：外部「题型构成」卡片删除；总题数由新外置的「题目数量」滑杆（10..100）承担
- **自动配比开关**（DataStore `exam_auto_mix`，默认开）：开启后按题库内各题型可用量占比自动分配总题数，只读展示「≈ N 题 · 库内 M」
- **手动比例滑杆**：关闭自动后每型一根 0..100%（步长 5）的比例滑杆，拖动某型其余按原比例等比让出份额（`setRatio`）；显示「60% ≈ 30 题」；实际题数 = 总题数 × 比例，clamp 到可用量
- `autoRatios` 辅助函数：按可用量折算百分比（兼作手动模式初始值）

### 模考 —— 修复与答题卡（第十八轮用户反馈）

- **高级选项展开文字重叠修复**：根因是 `AnimatedVisibility` 的内容布局把多个直接子级叠放在同一点绘制（含简答行/提示文字/拖拽列表全部叠画），包一层 `Column` 修复
- **答题卡按题型分段**：`buildSheetGroups` 按连续同题型分段（组卷本就按题型拼段），每段小标题「题型 · 第 X–Y 题 · 已答 x/y」，格子 FlowRow 摆放，替代单一平铺网格
- **题型排序说明**：列表下方注明当前题库参与排序的题型总数（解答"怎么只有两个可排序"——题库本就只有两种题型）

### 刷题 —— 填空题原位输入（第十八轮用户反馈）

- 新组件 `BlankInlineFields`：题干按 `_{3,}` 占位符拆段（与导入校验同口径），在空位处原位嵌入 `BasicTextField`（居中、下划浅底胶囊、空时显示"第 N 空"占位）；刷题/模考共用；脏数据兜底（题干无占位但答案有 N 空 → 末尾补输入位）
- 删除旧 `BlankAnswerFields`（第一空/第二空分行输入）；提交后逐空红绿着色，可接受答案见「正确答案」行

### 刷题 —— 其他（第十八轮用户反馈）

- **解析展开动画回归**：判定结果 + 解析区改 `AnimatedVisibility(visible = ua != null)`（expandVertically + fade），单选/判断、多选、填空、简答自评四处全覆盖；注意 AnimatedVisibility 必须常驻组合、由状态变化触发入场（`if` 包裹 + `visible=true` 不播动画）
- **提交按钮禁用态对比度**：surface 0.25→0.12 透明度 + 次级文字（原灰底灰字看不清）；导入弹窗「确认导入」同步
- **分类筛选移除**：刷题配置页删除「分类」区块（切题库即换内容，维度冗余）；`onStart` cat 恒传 "all"；概览「题库」总数改用 `Repo.bankCount`（不再经分类求和）；错题特训筛选不受影响
- **题型 chips 自动换行**：题目范围 Row+horizontalScroll → FlowRow

### 弹窗体系（第十八轮用户反馈）

- **OverlayBlur 引用计数**：boolean → 持有者 id 列表（push/pop），修叠层弹窗互踩——关闭「CSV 模板」对话框后仍开着的导入题库弹窗不再丢背景模糊
- **把手上拉过冲**：`rawDragY` 原始累计 + rubber-band 显示（上拉指数阻尼、上限 80px），松手弹簧回位；下滑关闭手感不变；`panelOffsetProvider` 允许负 translationY
- **收款码两态推移动画**：`AnimatedContent` 前进/反向转场（旧内容上滑出让位、新内容下方滑入）+ `animateContentSize` 高度平滑；导入弹窗两态同步加 animateContentSize
- **打赏文案动态化**：`formatUsage(usageMs)` 显示真实累计时长（"X 分钟 / X 小时 / X 小时 Y 分钟"），替代写死"2 小时"
- **性能降档**：内容层模糊 16dp/220ms → 12dp/160ms；壁纸层单独 8dp；弹窗面板玻璃 blurDp 24→18dp（模糊逐帧重算是掉帧主因）
- static_check must 断言 +8（examAutoMix / autoRatios / buildSheetGroups / BlankInlineFields / OverlayBlur.push / rawDragY / formatUsage）

## [2.8.2] - 2026-09-04

### 修复 —— 示例题库从未出现（第十七轮用户反馈）

- **根因**：`questions_sample.json` 的多选题用 `answers: [下标数组]` 写法，而 `buildBuiltinEntities` 只读 `answer` 位掩码 → 每道多选题校验都失败 → 整个示例题库播种静默失败（runCatching 吞掉），「生活常识示例题库」在任何设备上都从未出现
- **修复**：播种支持 `answers` 数组（折算为位掩码），与 JSON 导入通道口径一致；升级后启动自动补种

### 修复 —— 题库隔离（第十七轮用户反馈）

- **刷题配置页**：`LaunchedEffect(Unit)` 在首帧读到 `collectAsState` 默认值 `currentBank="drone"` 且永不重跑 → 切库后分类/题型/概览永远是旧题库（v2.7.3 首帧教训在 v2.8.0 的复现）。改为持续收集 settings 并按 `currentBank` 去重，首发射即 DataStore 真值、切库自动重载
- **模考配置页**：加载改挂起读 `settings.first()`，消除旧库闪帧
- **首页**：今日卡与近 7 天图表改为按当前题库统计（新增 `rowsByBankSince` JOIN 查询 + `last7DaysByBank`），卡片加「本题库」范围标记；打卡连击保持全局习惯数据；总览/错题/上次模考本就按库隔离，复核无误

### 体验打磨（第十七轮用户反馈）

- **弹窗把手去重**：GlassBottomSheet 自带把手，导入题库弹窗与打赏/收款码弹窗内容层又各画了一个 → 删除内容层重复把手
- **高级选项动画**：模考高级选项展开/收起加 `expandVertically/shrinkVertically + fade` 动画
- **首页题库切换菜单玻璃化**：`Popup` + 原生 surface → 新组件 `GlassAnchorMenu`（传送门渲染、折射「背景+内容」合成层、缩放淡入动画、锚定副标题下方）；副标题文本与问候语左对齐（去胶囊水平 padding）
- **模考题型构成**：加减号步进器 → 每题型一根滑杆（0..可用量、步长 1），显示 `N / 可用`
- **CSV 模板系统分享**：新增 `GalleryShare.shareTextFile`（cache/share + FileProvider 只读授权 + ACTION_SEND chooser），替代「保存到下载」；Manifest 注册 FileProvider（`@xml/file_paths` 仅暴露 cache/share/）
- **导入弹窗键盘避让**：内容区加 `imePadding + verticalScroll`，命名输入框不再被键盘遮挡
- **弹窗壁纸模糊**：AppRoot 背景层（壁纸/渐变）在 OverlayBlur 激活时与内容层一起真模糊，玻璃面板折射源整体成毛玻璃
- static_check must 断言 +5（answers 数组播种 / last7DaysByBank / GlassAnchorMenu / shareTextFile / imePadding）

## [2.8.1] - 2026-09-04

### 修复 —— 升级启动崩溃（v2.8.0(23) 老用户升级必崩）

- **根因**：`MIGRATION_1_2` 用 `ALTER TABLE ADD COLUMN` 给非空列附了 DEFAULT 值，而实体未声明 `@ColumnInfo(defaultValue=...)`，Room 打开库时逐列校验 schema 不匹配 → `IllegalStateException: Migration didn't properly handle`；且 v2 给 `ExamRecordEntity` 新增的 `Index("startedAt")` 迁移中未补建。SQLite 对非空列的 ADD COLUMN 必须带 DEFAULT，无法产出实体要求的 schema
- **修复**：迁移改为 Room 标准重建表模式——建 `_new_questions` / `_new_exam_records`（与实体完全一致、无 DEFAULT）→ `INSERT SELECT` 拷数据 → 删旧表 → 改名 → 重建全部索引（questions 的 category/type/bankId + exam_records 的 startedAt）；`exam_answers.detail`（可空 TEXT）保留 ADD COLUMN
- **数据安全**：Room 校验失败时 onUpgrade 事务整体回滚，崩溃过的设备库仍停在 v1 且未受损，覆盖安装本版后迁移自动重新执行；新增自动化验证脚本（真实 SQLite 上按 v1 建库播种 → 执行迁移 → 逐列逐索引比对 Room v2 期望 + 17 项数据保留断言）全部通过
- **防回归**：static_check 新增两条 must 断言（迁移重建表模式 / startedAt 索引）与一条 FAIL 级禁令（db 层 SQL 禁止非空列附 DEFAULT）

## [2.8.0] - 2026-09-04

### 新增 —— 多题库管理（用户第十六轮需求）

- **题库管理（设置 → 题库管理）**：所有题库统一列表（名称/题数/来源/当前使用高亮），点名称即切换；删除带二次确认（内置题库说明可用「清空记录」恢复，导入题库提示不可恢复）；删除当前使用中的题库自动切到剩余题库
- **内置示例题库「生活常识」**：20 题演示多选（6）/填空（6）/简答（4）/单选（2）/判断（2）；与无人机题库一样可删除，清空记录后恢复（墓碑机制：删除的内置题库不会被启动播种悄悄复活）
- **首页题库切换小弹窗**：副标题改为「每日精进 · 当前题库名 ▾」，点击弹出右键菜单式小窗——题库列表（✓ 标记当前）+「管理题库」直达设置
- **学习数据按题库隔离**（用户确认口径）：进度/错题本/统计/最近模考随当前题库切换；打卡与每日提醒保持全局习惯数据

### 新增 —— 导入体系升级

- **导入改底部弹窗**（设置 → 导入题库）：JSON / CSV 双通道选择，自动识别格式（含 BOM）
- **CSV 模板**：与《无人机装调题库_清洗版_含解析.csv》完全同构（题号/题型/题干/选项A-H/答案/解析/备注），并示范多选、填空、简答写法；支持查看 / 一键复制 / 保存到「下载/题屿」（MediaStore 免权限）——把内容丢给 AI 按模板整理即可自制题库
- **标准 CSV 解析器**：RFC4180 引号转义（题目内容含逗号/引号不裂行）、列头智能识别、题型别名归一（单选/多选/判断/填空/简答及常见变体）、答案宽容解析（字母/序号/对错正误）
- **导入报告**：解析先行——识别成功 N 题 + 失败行逐条原因（行号 + 错误说明，最多展示 8 条），失败不触碰数据库；题库名默认取文件名，确认后导入为独立题库并自动切换

### 新增 —— 新题型（多选 / 填空 / 简答）

- **多选题**：点选切换（方框勾选），手动「提交答案」后判定；**全对才算对**（错选/漏选都算错，用户确认口径），模考同口径计分
- **填空题**：题干 `____` 占位（3 个以上下划线记一空），逐空输入；页面根级 `imePadding` 键盘避让，提交按钮永不被键盘遮挡；提交后逐空对错 + 可接受答案展示；**精确匹配**（用户确认口径），模板支持一空多变体（`|` 分隔）
- **简答题**：多行草稿 → 提交看参考答案 → 自评「答对了/答错了」计入正确率（用户确认口径）；未自评不计分；模考中同流程，交卷时未自评计错
- **统一作答状态**：`UserAnswer`（picked 位掩码 + 文本 + 自评标记）贯穿刷题/模考/会话快照/数据库 detail 列，恢复进度后填空文本与简答草稿原样还原
- 题号面板新增「已答待判」状态（简答自评前），图例仅在该题型存在时展示

### 修复 —— 同轮反馈

- **错题特训不按筛选**（真根因：`onPractice` 回调根本不携带筛选参数）：修复后按当前题型/分类筛选刷错题，路由透传并 URL 编码分类名
- **模考单选/判断混排**（根因：组卷后整表 `shuffled`）：改为按题型分段抽题、按题型顺序拼卷
- **分类胶囊去重**：刷题页/错题本的分类 chips 不再显示与「全部」语义重复的单一分类（无人机装调）；题型筛选改为自适应 chips（只列当前题库真实拥有的题型）
- 搜索按当前题库过滤（与数据隔离口径一致），结果适配新题型展示

### 新增 —— 模考题型自适应与高级选项

- 题库只有单选+判断时：保留原「题目数量 + 判断占比」滑杆（无人机题库体验不变）；含新题型时：每型题数步进器（上限=题库实际拥有量）+ 总题数实时汇总
- **高级选项（默认折叠）**：含简答题开关（默认关，用户确认口径）；**题型顺序长按拖动自由排序**（`rememberUpdatedState` 规避拖动中手势重建，持久化 DataStore）
- 最近模考副标题按题型拼装（兼容旧记录的 extraCounts 空值回退）
- 成绩页错题分布按题型自适应展示；错题解析附「你的答案」对照

### 工程

- **Room 正式 Migration v1→v2**（告别人工破坏性升级）：新增 banks 表、questions 加 bankId/answerText、exam_records 加 bankId/extraCounts、exam_answers 加 detail；存量学习数据完整保留
- 题库删除的孤儿数据清理（practice_records/question_stats/wrongbook/exam 按 bankId 级联）
- DataStore 新键：current_bank / deleted_banks / exam_include_short / exam_type_order；PracticeSession 扩展 details 与 bankId（切库后旧会话自然作废）
- 设置 → 关于新增「请作者喝杯奶茶」入口（复用打赏弹窗；手动打开不标记"已弹"，不影响自动触达）

## [2.7.4] - 2026-09-03

### 新增 —— 用户第十四轮需求

- **支持作者（打赏弹窗）**：累计使用满 2 小时自动弹一次——"喜欢题屿吗？可以请作者喝杯奶茶"。点"看看收款码"展示打赏码（白底大图，透明底已垫白保证扫码对比度），一键"保存到相册"（存入 Pictures/题屿，MediaStore 无需权限，minSdk 31）；点"以后别提醒我"永久不再弹；外部关闭视为已弹过，不再打扰。**考试中绝不弹**——正在模考时达标会等考试结束再出现
- **模考历史记录可回看**：考试页点击已完成的历史记录即可再次进入该场模考成绩页——成绩不再依赖交卷瞬间的内存快照，改为从数据库按 examId 精确重建（每题作答本就落库）
- **模考成绩页删除入口**：右上角浅色"三点"按钮 → 删除本次模考记录（成绩+作答一并清除）；**每周限删 2 次**，确认弹窗实时显示本周剩余额度，用完弹窗告知"下周一自动恢复"

### 修复 —— 同轮反馈

- **顺序/随机进度按模式分开记（双槽）**：顺序会话与随机会话各存各的进度槽，互不覆盖——顺序模式接着顺序的上次位置，随机模式接着随机那轮的位置，切换模式提示行实时跟随；错题特训覆写快照的隔离（v2.7.3）保持
- **模考成绩页错题解析滑动卡顿**：展开后 20+ 条解析全量组合在外层滚动 Column 里（外加玻璃卡逐帧采样），每帧测量绘制量巨大；改为 LazyColumn 懒加载列表（固定上限高度、只组合可见条目、item 复用、q.id 作 key），长列表滑动不再卡，滚到边界自动衔接外层滚动；顺带放开只显示前 20 条的限制
- **"重新开始"按钮重做**：裸红色文字改为浅红底描边胶囊（不再突兀）；点击先弹二次确认（说明将清空当前模式的进度与作答、不可撤销），确认后才执行——误触不再直接清掉进度

## [2.7.3] - 2026-09-03

### 修复 —— 用户第十三轮实测反馈

- **随机刷题终于"真随机"（双重根因同时拆除）**：
  1. `Repo.loadPractice` 中 `ids.shuffled()` 后经 Room `IN (:ids)` 查询——Room 固定按主键升序返回，洗牌顺序被完全吃掉，随机列表实际就是题库顺序（"随机和顺序一模一样"的直接根因）→ 改为按洗牌后的 ids 保序重排；
  2. 刷题页 load 协程捕获的 `settings` 是 `collectAsState` 首帧默认值（`practiceOrder=0`），DataStore 尚未发射时新开列表永远按顺序加载 → 改为协程内挂起读 DataStore 真值
- **刷题进度接续三重加固**：
  1. 错题特训不再覆写刷题会话快照（此前快照仅一份全局槽位，特训一开就把主刷题进度冲掉，回配置页再开刷判定失败、从头开始）；
  2. 恢复路径此前已在 v2.7.2 引入 `restoring` 窗口抑制恢复期落盘，本轮保留；
  3. 跨模式（顺序↔随机切换）接续进度语义保持：有未完成会话就恢复列表＋作答＋位置，刷完自然开新一轮
- **APP 更名「题屿」**：不再使用"无人机题库/无人机装调题库"类名称——launcher 名、设置-关于同步更换；每日提醒通知文案去掉"装调"字样
- **错题本筛选行与列表间距**：筛选胶囊行底部留 12dp 固定空隙，卡片滚动到顶不再贴边擦蹭（用户截图圈注）
- **滚动把手样式微调**：拇指 4→5dp、静态存在感 32%→40%（更清晰好认），拖动提亮 72%；像素级连续映射逻辑不动（用户确认流畅性达标）

### 优化 —— 设置页文案

- **阅读字体**：描述小字精简为四字短评（手机自带/清晰耐读/书卷气息/温润手写），不再换行；示例文本改为通用的"这是一段示例文本 Aa 0123"，不再使用考证术语
- **每日提醒**：说明小字精简为"已开启 · 刷过当天不打扰 / 每天定时提醒 · 刷过不打扰"，不再换行

## [2.7.2] - 2026-09-03

### 修复 —— 用户第十二轮实测反馈

- **顶部羽化正式砍除**：五版迭代（布尔蒙版 → draw 直读 → 雾条 → 动态离屏 → 生长式蒙版）后用户实测裁定"还是失败了……砍掉这个吧"。功能下线：六页滚动容器（BounceLazyColumn / BounceContainer / LazyColumn 均自带固定边界裁剪）内容滚出即在容器上缘干净直切，无蒙版、无离屏、无渐变；答题卡与题号网格的 `softVerticalEdges`（纯色格子，无玻璃互作）不受影响
- **错题本滚动把手 v3（齿轮感根除）**：v2 跟随只读 `firstVisibleItemIndex`（每跨一题跳一格，卡片高百余 dp）＋拖动 `scrollToItem(index)`（整题整题地跳），故"一跳一跳像齿轮、拖动不像列表在滚"。v3 全链路改像素级连续映射：跟随 = 已滚出像素 / 可滚总像素（首 item 的 index+offset 换算，逐像素平滑），拖动 = 同一模型反向映射（`scrollToItem(index, offset)` 亚题精度）；样式收敛 4dp×44dp 低存在感胶囊，拖动放大 1.25 倍＋提亮
- **搜索结果"绿色选项字母不显示"**：正确项徽章绿底配绿字同色隐形，改白字；刷题页选项徽章同步恢复显示原字母（白字），不再替换成 ✓/✕ 图标
- **刷题进度"每次点开都是第一题"**：恢复链路竞态——翻页持久化的 `snapshotFlow` 初始发射把快照 index 覆写回 0，恢复跳页读到的是被覆写后的 0。引入 `restoring` 窗口抑制恢复期落盘，落盘只发生在真实翻页/作答时；上一轮已全部刷完的会话不再接续（随机模式自然重新洗牌）
- **昵称输入框**：光标位置随文字起点（此前 `TextAlign.End` 光标顶到最右）；宽度 140→112dp；存储层截断与 UI 对齐（take 12→5）
- **首页总进度卡**：顶部留 6dp，玻璃卡上溢阴影不再被容器上缘裁切（用户：留一点点像素就可以）

### 新增

- **内置阅读字体**：设置-外观新增"阅读字体"——系统默认 / 思源黑体 / 思源宋体 / 霞鹜文楷，切换全局即时生效（MaterialTheme typography 全量注入，BasicTextField 显式接入）；字体经 GB2312＋题库全字符集子集化（合计约 17MB），OFL 许可随包附于 `assets/FONT_LICENSES.txt`；设置页附示例行所见即所得
- **搜索解析展开动画**：搜索结果点开的选项与解析弹性展开/收起（spring expandVertically），替代直出直隐
- **续刷提示**：刷题配置页同筛选下存在未完成进度时显示"将接续上次进度 · 第 N / M 题"，附"重新开始"一键清除快照

## [2.7.1] - 2026-09-03

### 修复 —— 羽化过渡打磨（用户第十一轮实测反馈，视频逐帧定位）

- **羽化 v4"生长式蒙版"**：v3 的过渡病灶——strength 作为整条渐变曲线的 alpha 缩放系数，滚出 10px 时首卡整个顶部区域被统一淡化 15%，内容"还没到边缘就提前变淡"。重做为：蒙版带高度 = min(已滚出像素, 36dp)，**滚到哪里、淡到哪里**，未达边缘的内容 alpha 恒为 1；蒙版底端 alpha 恒为 1 与下方内容天然无缝；smoothstep 曲线替代线性（消除"被幕布切"感）；稳态带加高 26→36dp；答题卡 `softVerticalEdges` 同步同款模型
- **搜索框 Hero 动画真正生效**：根因——`LocalSharedTransitionScope` 从未 provide，`heroSearchField()` 永远走 null 兜底、sharedElement 从未挂上；现于 `SharedTransitionLayout` 内注入 scope；刷题页搜索框圆角 20→22 与搜索页统一（飞行衔接更连贯）
- **输入光标**：搜索框与昵称框 cursorBrush 从 accent 亮橙改为正文墨色（用户反馈扎眼）

### 变更

- **昵称与问候**：默认不再称呼"机长"——未设置昵称时首页只按时间问候；昵称可自定义最长 5 字（输入即截断）；说明文字去掉"最多 X 字"备注；保存改显式"确认"按钮（替代 600ms 防抖自动保存），placeholder 改"未设置"
- **首页双卡排版统一**：连击卡与今日卡完全同构五行走线（图标+标签 / 大数字 / 副行 / 进度条 / 底注），字号（20sp/12sp/11sp/10sp）、间距、条高（5dp）逐行对齐，仅图标（🔥/✓）与条色（橙/绿）按语义区分

## [2.7.0] - 2026-09-03

### 修复 —— 顶部羽化 v3：saveLayer 重做（用户第十轮实测反馈，视频定位）

- **"整页内容变淡"的多帧闪烁（灾难级）**：用户录屏（逐帧分析 00:05~00:09 多帧）暴露 v2.6.4 动态离屏的致命缺陷——`compositingStrategy` 在 Auto↔Offscreen 阈值穿越时 graphicsLayer 整层重建，重建帧内容未就绪即合成。彻底重做：**不再持有跨帧 graphicsLayer**，改为单次 draw pass 内 `canvas.saveLayer → drawContent → DstIn 蒙版 → restore`——layer 每帧独立创建销毁、无跨帧状态，物理上不存在重建闪烁；仅滚离顶部需要蒙版时才 saveLayer（顶部静止零开销直绘，玻璃卡上溢高光/阴影完整）；saveLayer 边界四周扩 24dp，滚入羽化区的玻璃卡上溢绘制被保留

### 新增

- **首页问候语 + 昵称**：大标题按时间问候（早上好/中午好/下午好/晚上好）+ 昵称（默认"机长"）；设置-外观顶部可设置昵称（600ms 防抖保存，最长 12 字）
- **搜索历史**：DataStore 持久化最近 8 条（去重置顶）；空查询时流式胶囊展示，点按即搜、可清空；有结果时自动记录
- **搜索框 Hero 动画**：`SharedTransitionLayout` + 共享元素——刷题页搜索入口与搜索页输入框同 key，导航转场时平滑飞行衔接（ExperimentalSharedTransitionApi）

### 变更

- **首页"今日"卡片重排**：题量/正确率 + 答对/答错双色占比条（`Spacer(weight)` 撑满对齐连击卡），不再空洞
- **设置分区重排**：刷题提到全局壁纸之前（刷题为主，先看到常用的）
- **错题本筛选胶囊微交互**：按压缩放（0.88 spring 回弹）+ 背景/边框/文字颜色 spring 渐变过渡（替换"秒变黑"）
- **错题本滚动把手 v2**：触控轨道 44dp 全高（好抓）、拇指 6dp×56dp 拖动时放大 1.3 倍+高亮、**拖动改绝对映射**（按下锁定基准+增量累加，消除旧版以列表位置回算基准的自反馈漂移抖动）、点按轨道直接跳转

## [2.6.4] - 2026-09-03

### 修复 —— 柔化方案回调（雾条废弃）+ 过冲越界（用户第九轮实测反馈）

- **雾条方案废弃，回归蒙版柔化**：v2.6.3 雾条在壁纸模式下与背景色差大（截图出现明显色带、顶部有硬边）、且滚离顶部后盖住玻璃卡上缘——用户裁定"还不如上一次"。恢复"内容渐隐蒙版"（颜色无缝、视觉正确），并修复蒙版方案的两个老毛病：① 闪——强度依旧 draw 阶段 lambda 直读（Modifier 稳定零重建）；② **首卡上阴影被截断的伪影（真根因）**——此前 `compositingStrategy=Offscreen` 无条件开启，即使停在顶部（不需要柔化），玻璃卡上溢的高光/阴影也被离屏层边界裁掉；现改为**动态离屏**：仅滚离顶部需要蒙版时才离屏，顶部静止时普通合成、阴影完整
- **过冲越界修复**：上拉/下拉过冲回弹时列表内容越出容器、盖住固定标题/底栏（用户截图确认）——BounceLazyColumn/BounceContainer 增加固定边界裁剪 `clipToBounds()`（在位移层之外声明，裁剪边界不随回弹位移），过冲内容在容器边缘被裁、顶部露背景（标准 iOS rubber-band 观感）

## [2.6.3] - 2026-09-03

### 修复 —— 伪影根除（柔化 V2）+ 壁纸更换（用户第八轮实测反馈）

- **标题柔化伪影根除（雾条方案）**：v2.6.2 的 Modifier 稳定化只消除了"每帧重建"因素，用户实测确认伪影"依然存在，只是减轻了一点点"——确认剩余伪影来自**离屏蒙版层与列表内液态玻璃卡折射层的方案级互作**（DstIn 蒙版必须离屏渲染整个列表，玻璃卡的折射像在半透明区域产生重边/色块，无法从外部修复）。本版彻底换方案：五页标题柔化从"列表渐隐蒙版"改为**标题下缘雾条**——一条背景色渐变（亮米白/深墨，壁纸模式取纱色）盖在内容之上，随滚动距离连续渐显（跟手）；零离屏、零蒙版、零 graphicsLayer，与玻璃卡物理上无任何互作，**必然无伪影**。停在顶部时无雾、首屏内容不被遮挡的语义保持不变；答题卡题号网格（纯色格子、无玻璃卡）保留原 softVerticalEdges 蒙版
- **修复：更换壁纸不生效**——此前每次导入都写入固定的 `wallpaper.jpg`，DataStore 存储的路径字符串不变，背景层 `LaunchedEffect(settings.wallpaper)` 不重新触发，永远显示第一次导入的图。现在导入与内置壁纸均使用**带时间戳的唯一文件名**（路径必变 → 必然重新解码），导入成功后自动清理旧壁纸文件

### 新增

- **内置壁纸**：设置-全局壁纸新增四个内置纹路缩略图——**山野清晨**（扁平山景·浅）/ **林海深处**（扁平山景·深）/ **云上航线**（云层油画）/ **黄昏原野**（暮色原野），点击即设为全局背景（复制进私有目录，走同一条加载链路，支持模糊/文字纱/玻璃折射），当前选中项橙描边标记

## [2.6.2] - 2026-09-03

### 修复 —— 柔化伪影与突变（用户第七轮实测反馈；惯性修复 v5 本轮确认有效）

- **柔化"要么有要么没有"+出现时闪一下+重影伪影（同根三症）**：此前柔化强度经 `animateFloatAsState` 驱动、以参数形式传入 `softTopFade`/`softVerticalEdges`——强度每帧变化都生成**全新 Modifier 实例**，`graphicsLayer(Offscreen)` 与 `drawWithContent` 节点随之每帧销毁重建：重建瞬间旧离屏层残影=**重影伪影**（截图卡片两角色块）、重建黑帧=**"所有东西消失 0.05 秒"**、渐变被闪烁淹没=**突变感**。重写为官方 *defer state reads* 模式：强度改为 **draw 阶段经 lambda 读取**（`scrolledPx: () -> Float`），Modifier 实例全程稳定零重建，状态变化只触发重绘——伪影与闪烁根除
- **渐显不再有固定 180ms 动画延迟**：强度直接跟随"顶部已滚出像素 / fadeHeight"**连续**变化——滑出多少渐显多少、跟手无延迟，真正的"渐渐出来"；贴顶/贴底时依旧完全无柔化。配套新增 `LazyListState/LazyGridState/ScrollState.scrolledFromTopPx()` 与 `LazyGridState.remainingBottomPx()`（底部剩余像素近似，供答题卡网格底部柔化连续渐显）
- 清理 6 个文件中的未用动画 import

## [2.6.1] - 2026-09-03

### 修复 —— 滚动惯性真根因（用户第六轮实测反馈）

- **"上下滑动没有惯性，一松手它就停了"（连续三轮未修透的真根因）**：经 GitHub/官方文档调研确认，`NestedScrollConnection.onPreFling` 的返回值语义是**本层消费的速度**，未消费的部分才交还子列表继续 fling（sinasamaki 原文未过冲时 `return super.onPreFling(available)` = 消费 0）。此前 v3/v4 把"要交还的速度"当返回值写成了 `return available`——语义完全颠倒，等于把普通 fling 的全部速度一口吞掉：列表收到 0 速度，松手即停。v5 将三处返回值全部修正：未过冲 → 消费 0（普通甩动零干扰、惯性完整保留）；过冲 + 强甩向内容侧 → 消费 0（速度全部交还，惯性立即生效），过冲量异步摩擦衰减并行归零；甩不回/往外甩 → 消费全部垂直速度，交给异步软弹簧回弹
- **fling 途中撞边的过冲量挂住不回弹**：新增 `onPostFling` 兜底——fling 自然结束后，剩余过冲量带残余速度弹簧回弹（onPreFling 已启动的动画不打断）
- **柔化遮挡内容（主页进度环被渐变裁切）**：`softTopFade`/`softVerticalEdges` 增加 0..1 强度参数，绑定列表/网格的 `canScrollBackward`/`canScrollForward`（180ms 渐变过渡）——停在顶部/底部时完全无柔化，首屏内容（进度环、筛选行、首条结果）不再被渐变遮住；开始滚动后柔化淡入，停在中间时保持。覆盖：四个 Tab 页固定标题、搜索结果列表、模考/刷题答题卡题号网格
- **主页连击卡与今日卡高度不齐**：双卡 Row 改 `IntrinsicSize.Min` + 两卡 `fillMaxHeight`，等高对齐
- **自定义壁纸下文字看不清**：壁纸层叠加主题纱（亮色叠米白纱、深色叠墨色纱，开模糊时更透），任意壁纸/主题组合下文字对比度恒定可读

### 变更

- **每日提醒智能化**：不再固定每天 20:00——自动学习用户习惯，取最近 10 天每天首次刷题时刻的中位数（夹在 10:00–21:30，无数据默认 19:30）作为提醒时刻；**当天已刷过题则保持安静**；调度改为 OneTime + Worker 自续（每天动态重算时刻）；去掉"首次开启需通知权限""未授予通知权限…"等说教文案，拒绝权限时静默不置位
- **底栏"刷题"图标重绘**：去掉旧版怪异的后卡帽（视觉上像歪掉的盖子），改为与错题本"方块+X"成对的"方块+对勾"填充式图标，Tab 栏视觉语言统一

## [2.6.0] - 2026-09-03

### 修复 —— 惯性丢失 + 刷题记录（用户第五轮实测反馈）

- **上下滚动"没有惯性，停止滑动它就停"**：根因是过冲量存放在 `Animatable` 里，拖动/惯性每帧经 `scope.launch { snapTo }` 异步写入——fling 结束的 `onPostFling` 与排队中的写入存在时序竞态：读到旧值 0 就跳过回弹、随后写入又把过冲量置回非零 → **过冲量残留**；此后每次松手 `onPreFling` 都误入过冲分支并**同步挂起数百毫秒**（软弹簧动画），fling 被推迟到回弹结束才开始，体感即"没惯性"。v4 重构：过冲量改为 `mutableFloatStateOf` **同步直写**（零协程零竞态），`onPreFling` 内**零挂起点**立即返回——普通滚动惯性完全不受影响；过冲时向内容侧强甩则全部速度立刻交还列表（惯性立即生效），过冲量异步摩擦衰减并行归零
- **刷过的题没记录**：普通刷题再次开始时自动接续上次会话——按筛选参数匹配快照，恢复题目顺序/已答对错标记/进度页码（换筛选条件=开新会话）；配合既有的逐题落盘，"今日已刷 N 题"与实际作答完全对得上

### 新增

- **题目搜索**：刷题 Tab 顶部新增搜索入口（题干/选项/解析全文 LIKE，防抖 300ms，上限 80 条）；结果点开可看全部选项、正确答案与解析
- **全局壁纸**：设置-外观可导入图片作全局背景"纹路"（复制到私有目录持久化），可开启**壁纸模糊**、可一键恢复默认；壁纸经背景记录层进入所有玻璃组件的折射
- **底栏玻璃模糊度三档**：低（4dp）/中（8dp）/高（14dp），设置-外观可调
- **错题本筛选 + 滚动把手**：顶部固定筛选行（全部/单选/判断/动态分类 chips）；右侧快速滚动把手（按比例映射列表位置，题目少于 8 条自动隐藏）

### 优化 —— 体验细化

- **答题卡/题目面板**：移除深色 scrim（内容层真模糊已足够层次，用户反馈"没必要加深色遮罩"）；顶部新增小把手，**拖动下滑可关闭**（超阈值松手关闭、否则弹性回位）；题号网格**上下边缘柔化渐隐**（DstIn alpha 蒙版），替代硬切行
- **标题固定 + 柔化**：首页/刷题/模考/设置四页标题固定顶部不随滚动（对齐错题本），滚动内容在标题下缘 alpha 渐隐（不依赖背景色，壁纸模式下同样干净）
- **图标填充式**：Tab 栏五图标与答题卡/垃圾桶/通知铃/火焰/导入/漏斗/搜索/播放/对勾/关闭等全部重绘为填充路径（此前为描边线条式）
- **深色模式开关对比度**：开启态轨道为奶色时钮身同步转深（材质/玻璃两种模式），不再白底白钮难以辨认
- **每日打卡活力化**：动态激励文案（按连击阶段变化）+ 里程碑进度条（3/7/14/21/30/50/100 天，显示"再练 X 天达成 N 天连击"）
- **预估通过率参考及格分**：公式改为 `正确率相对及格线达标度×0.7 + 覆盖率×0.3`，调整设置中的及格分立即重算（此前固定阈值无反馈）

## [2.5.1] - 2026-09-03

### 修复 —— 手感与视觉伪影专项（用户第四轮实测反馈）

- **上下滚动"跟手不顺畅"（回弹 v3，按社区标准实现重写）**：调研 sinasamaki《Overscroll animations in Jetpack Compose》标准方案后定位三处根因——① 显示曲线起手斜率仅 0.55（一起手就钝 45%），换 **CubicBezier(0.5, 0.5, 1.0, 0.25)**：起手斜率恰为 1:1 绝对跟手、渐进变韧；② 过冲渐近线仅 110dp，改为**容器高度**（iOS 同款用 dimension）；③ 回弹把松手速度直接当弹簧初速度且无钳制，会甩过头荡到另一侧。甩动衔接改为 **exponentialDecay 预测**：甩不回内容侧→软弹簧（StiffnessLow）带速度回弹；会冲过零→过冲量摩擦滑行、过零瞬间 snapTo(0)、**剩余速度原样交还列表**（iOS 式无缝衔接，不再吞甩动）
- **底栏"方框/圆环"伪影**：选中块按压鼓包 `pressedScale 78/56≈1.39`，放大后 56→78dp，超出 64dp 栏体上下各 7dp，透镜色散在栏外画出一圈带彩虹边的圆环（截图可见）；收敛为 **62/56** 并将选中块透镜 10/14dp 降至 8/11dp。同时移除 v2.5.0 加在底栏上的 `Modifier.blur`——方形模糊层在胶囊四周留下方框裁剪痕迹（底栏玻璃折射的本来就是已模糊的内容层，无需再套）
- **左右切题"上下被切一刀"+阴影感**：v2.5.0 的 3D 转盘（rotationY+透视相机+alpha 渐隐）会让卡片上下角超出 Pager 视口被裁剪出水平切线，且 3D 变换与玻璃 backdrop 采样叠加产生阴影状伪影；移除旋转与渐隐，仅保留轻微纵深缩放（离场页 1→0.97），干净无伪影

### 变更

- **应用图标更换**：采用用户设计图（米色底 + 白色圆角卡 + 黑色对勾 + 橙色线条）——自适应图标前景改为按内容包围盒裁剪的位图（mdpi~xxxhdpi 五档，内容收在 66% 安全区），背景色同步取样为 `#F3EFE3`，移除旧无人机矢量前景

## [2.5.0] - 2026-09-03

### 修复 —— 弹窗被自身模糊 + 界面整理（用户第三轮实测反馈）

- **答题卡/弹窗展开时面板自身也被模糊**：v2.4.0 的 `OverlayBlur` 对整个内容层施加模糊，而弹窗面板就渲染在内容层内，导致"答题卡打开后答题卡自己也是糊的"。现引入**弹窗传送门（GlassOverlayPortal）**：面板统一由 AppRoot 在内容模糊区与底栏之上渲染，永远清晰；多槽位列表支持同屏多个弹窗互不覆盖（如模考页确认框 + 答题卡）
- **弹窗折射升级**：面板位于内容记录层之外后，可安全折射 `combined(背景层, 内容层)`——打开弹窗时折射的正是"被模糊的内容"，更接近 iOS 液态玻璃；此前因循环采样风险只能折射背景渐变层
- **底栏模糊一致性**：弹窗打开时底栏同入模糊区（iOS 同款：面板之外一切虚化），不再出现"内容糊了、底栏清晰"的割裂感
- **首页卡片贴叠**：错题本/上次模考两张信息卡此前无纵向间距（LazyColumn 相邻 item 直接相接），液态玻璃透镜边缘让两张卡看起来融在一起；现统一间距（12dp）与圆角规格（22dp），样式一致
- **错题本类目标签标红**："无人机装调"等类目标签此前用错误红色，视觉上像出错强调；改回中性 `textSub` 低调节，红色只留给错误语义（"错 N 次"等）
- **错题本右上角重复入口**：移除纯图标"开始特训"按钮（与列表顶部"图案+字"按钮重复），只保留一个入口

### 变更 —— 全局手感升级

- **上下过冲回弹重写（BounceState）**：修复此前实现"不够丝滑"的三处根因——① 90ms 定时器兜底会在按住不动/慢速拖动时与手势抢状态造成抖动（最大元凶），改为 `onPreFling`（拖动松手）+ `onPostFling`（fling 惯性撞边结束）双完备触发点；② 位移改用 iOS UIScrollView 同款 rubber-band 曲线 `d·(1−1/(x·c/d+1))`，虚拟位移与显示位移分离，反向拖动严格沿原曲线返回；③ 回弹弹簧改临界阻尼（`dampingRatio=1f`），无过零震荡，松手带初速度顺势回弹——"豆腐般"软糯
- **设置页补齐回弹**：设置页此前是硬边界（无过冲动画），现包 `BounceContainer` 与其他页面一致
- **刷题左右切题转盘效果**：题目 Pager 页面加 `graphicsLayer` 变换——`rotationY`（绕竖轴随滑动偏转 20°）+ 透视相机（`cameraDistance`）+ 弧面压缩（邻页向圆心轻收）+ 轻微缩放淡出，切换时产生"转盘半径"纵深感
- **模考配置页紧凑化**：题目数量/判断题占比/考试时长/及格分四张全宽大卡占满整屏，孰轻孰重失衡；压缩为 **2×2 紧凑设置格**（标签+当前值+迷你滑杆），"开始考试"与"最近模考"首屏即可达
- **移除刷题页"继续上次"卡片**：与错题本练习入口重复且易混淆（会同步显示错题特训会话）；刷题进度快照仍在后台静默保存，后续需要时可低成本恢复 UI

## [2.4.0] - 2026-09-03

### 修复 —— 底栏导航根因修复 + 刷题交互重构（用户实测反馈）

- **底栏拖拽松手不切页（根因级修复）**：`DampedDragAnimation` 被 `remember` 缓存，其 `onDragStopped` 闭包捕获了首次组合时的 `onTabSelected` → 旧 `navigateTab` → 旧 `tabIndex`。从首页出发后闭包内 tabIndex 恒为 0，拖回首页松手时 `if (index == tabIndex) return` 误判吞掉切换——表现为"高亮块回去了、页面没回去"、从设置页点不回首页。现改用 `rememberUpdatedState` 让拖拽回调始终拿最新引用，`onDragStopped` 直接同步回调切页（不再依赖 `snapshotFlow`/动画链），`currentIndex` 快照流整体删除，页面变化仅驱动高亮块动画
- **首页右上角进设置与底栏行为统一**：`onSettings` 改走 `navigateTab(4)`（保存页面状态、栈结构可预测），彻底消除"页面叠加"体感
- **刷题页被底栏遮挡**：刷题 Tab 重构为**配置入口页**（参考模考配置页布局：题型范围/分类/顺序卡片 + 开始按钮 + 学习概览统计），点"开始刷题"进入**全屏刷题页**（非 tab destination，无底栏遮挡滑杆与左右切题按钮）；错题特训同步走全屏模式
- **普通刷题记录丢失（双根因）**：① 作答记录挂页面级 `rememberCoroutineScope`，答完立刻退出时协程被取消——改挂应用级 `ServiceLocator.appScope`；② 答题卡进度只存内存，页面销毁即丢——新增 `PracticeSession` 快照（DataStore 持久化题目顺序/已答/页码），答题与翻页实时落盘，配置页"继续上次刷题"一键恢复；题库升级与"清空记录"时同步作废
- **模考"进行中"幽灵记录**：放弃考试此前只 `popBackStack`，DB 残留 `score=null` 记录且点击无响应、无法删除。现在：放弃即删（`Repo.abandonExam` 事务删除记录与作答）；未完成模考支持**继续考试**（内存会话优先，进程重启后从 DB 重建题目/已答/剩余时间）与**删除**（垃圾桶按钮）；"清空做题记录"补充清空 `exam_records`/`exam_answers`
- **模考/刷题题目卡片内容溢出**：题目卡内部增加 `verticalScroll`，长题目与解析展开后可上下滚动查看，不再被裁切在屏幕外
- **弹窗背景"黑遮罩"体感**：`GlassOverlay` 打开时通过 `OverlayBlur` 全局状态对内容层施加真实模糊（Android 12+ RenderEffect，低版本自动降级为原效果），scrim 由 28%/45% 减淡至 10%/22% 仅作层次；面板 `surfaceColor` 透明度 0.9→0.62，折射质感可见

### 变更

- 新增路由 `practiceRun?src={src}&type={type}&cat={cat}&resume={resume}`（全屏刷题）；`practice` tab route 仅承载配置页
- `ServiceLocator` 新增 `appScope`（应用级协程域，落盘操作不随页面销毁取消）
- `Repo` 新增 `abandonExam` / `resumeExam` / `loadPracticeByIds`；`ExamDao` 新增按 id 删除
- `SettingsStore` 新增 `practiceSession`（kotlinx.serialization JSON 持久化）

## [2.3.2] - 2026-09-03

### 修复 —— 交互与功能批量修复（用户实测反馈）

- **底栏无法单击切换**：`TabIconSlot` 缺少点击处理（官方 `LiquidBottomTab` 有 `clickable(Role.Tab)`）。现单击任意 tab 图标直接跳页；隐藏染色层通过 `LocalTabClickEnabled` 禁点避免双触发
- **拖拽切换概率失效**：`onTabSelected` 原排在 `animateToValue` 完成之后，且 `collectLatest` 会在连续操作时取消前序动画协程 → 回调丢失（表现为"滑完了页面没切"、切不回主页）。现回调提前到动画之前，动画只承担视觉
- **"一直加载题库"加固**：刷题加载增加 10s 超时 + 异常捕获 + 失败重试按钮；题数 0→N（后台导入完成）自动重载；启动门控最多等 8s 即放行主界面，导入转入后台继续并自行持久化版本
- **滑杆只能点不能拖**：原拖拽手势不消费指针事件，配置页/设置页内会被父级 `verticalScroll` 抢走；进度类滑杆（模考/刷题）拖动时外部值回写与同步循环互相拉扯（橡皮筋）。现重写为轨道区统一手势（按下跳转、按住拖动实时跟随、事件全消费）、拖拽中暂停同步循环、触控区 24dp→40dp
- **模考题目垂直居中**：`HorizontalPager` 默认 `CenterVertically`，改为 `Alignment.Top` 顶部对齐（刷题页同步受益）
- **模考底部滑杆贴手势条**：底部操作条补 `navigationBarsPadding`，滑杆与左右按钮间距 8dp→14dp（刷题页同步处理）
- **模考新增答题卡面板**：顶栏网格按钮打开，玻璃化 `GlassBottomSheet`（与刷题页题号面板同款），已答/未答区分、点题号直接跳题
- **每日打卡通知无权限逻辑**：Android 13+ 开启开关时运行时请求 `POST_NOTIFICATIONS`；授予后排程提醒，拒绝时给出明确提示；副标题显示开启/权限状态
- 底栏 `selectedTabIndex` lambda 改用 `rememberUpdatedState`，避免重组导致内部状态重置

## [2.3.1] - 2026-09-03

### 修复 —— v2.3.0 启动即闪退（热修复）

- **崩溃根因**：v2.3.0 将 shapes 从 maven 依赖改为源码 vendor 时，`com.kyant.backdrop.effects.Lens` 中的 `cornerRadii` 分支被一并裁掉了 `RoundedRectangularShape`（Kyant Shapes）分支；而底栏、按钮、滑杆、开关全部使用官方 `Capsule()` 胶囊（实现 `RoundedRectangularShape`）→ 首帧组合即抛 `UnsupportedOperationException: Only RoundedRectangularShape or CornerBasedShape is supported in lens effects.`，应用启动即异常退出
- **修复**：补回 `RoundedRectangularShape` 分支（`shape.corners(size, layoutDirection, density)` 取四角半径，与上游 AndroidLiquidGlass 完全一致），Capsule 连续曲率胶囊的折射效果恢复正常
- **加固**：`lens()` 遇到真正不支持的形状时由硬抛异常改为**优雅降级**——跳过折射、保留已有模糊，任何形状都不会再导致崩溃；与既有的 AGSL 着色器编译失败保底（runCatching）形成双保险

## [2.3.0] - 2026-09-03

### 重构 —— 液态玻璃全面对齐官方（Kyant0/AndroidLiquidGlass）

- **架构修复（核心）**：`AppRoot` 由"单记录层包内容"改为**双记录层**——`bgBackdrop` 只记录背景渐变（官方 demo 的"壁纸"角色），`contentBackdrop` 记录内容。此前玻璃折射采样的是大面透明像素（背景在记录层之外），导致"看不出折射"；现在：
  - 内容流卡片/按钮/滑杆/开关全部升级为**真折射玻璃**（官方 `LazyScrollContainerContent` 同款模式：元素不在背景记录层内，零循环采样、零 SIGSEGV 风险）
  - 底栏折射 `bgBackdrop + contentBackdrop` 组合层，滚动内容从底栏下穿过时透过玻璃可见
- **Capsule 连续曲率胶囊**：官方 shapes（Kyant0/Shapes，Apache-2.0）源码 vendor 到 `com.kyant.shapes`（maven 产物要求 compileSdk 37 + AGP 9.1 并连带升级 Compose，暂不跟进；源码仅依赖 compose-ui 可直接内联），底栏三层结构、按钮、滑杆轨道/滑块、开关轨道/圆钮全部替换为官方同款 `Capsule()`，折射边缘更柔润
- **开关重做**：`GlassToggle` 按官方 `LiquidToggle` 逐行对齐——玻璃圆钮可拖拽、按压膨胀、轨道颜色随状态渐变；明确不可加外层 clickable（`inspectDragGestures` 不消费事件，避免双触发）
- **滑杆对齐官方 `LiquidSlider`**：滑块折射"背景+轨道放大"（`rememberBackdrop` 自定义绘制）、按压色差镜片、点击热区由 6dp 轨道扩大到 24dp 容器
- **弹窗玻璃化**：题号面板（原 `ModalBottomSheet`）与全部确认对话框（原 `AlertDialog`）替换为同窗口自绘玻璃组件 `GlassBottomSheet` / `GlassConfirmDialog`——独立窗口无法采样主窗口 backdrop，故必须同窗绘制；遮罩点按/返回键关闭，面板内点按消费防误关
- 安全模式降级链路保留：特效关闭或异常退出自动降级时，所有玻璃组件退回质感材质（无 RenderEffect）

### 修复

- **"一直加载中"**（`Repo.loadWrongPractice` / `loadPractice`）：Room 对空列表 `IN ()` 生成非法 SQL，错题本为空时进"错题特训"、筛选无结果时协程静默死亡，页面永远"正在加载题库…"。现已空列表短路防御（`startExam` 空库同样防御）
- **模考统计污染**（`Repo.submitExam`）：`bumpStreak(correctIds.size >= 0)` 恒为 true——全错也计"正确+1"且 50 题模考只计 1 次答题。改为 `bumpStreakBulk(答题数, 正确数)` 按实际累计
- **题库导入死锁**（`Repo.ensureBankLoaded`）：此前导入失败仍返回版本号并被持久化，空库状态下永不重试。现在解析/校验先行（失败不碰数据库），"清学习数据+换题库"并入单事务，只有完全成功才返回版本号，失败下次启动自动重试

### 变更

- `GlassToggle` 签名对齐官方：`checked: () -> Boolean`（避免 remember 闭包捕获过期状态）
- `GlassCard` / `GlassButton` / `GlassIconButton` 默认 `refracts = true`

### 工程

- vendor 官方 Kyant0/Shapes 源码（Apache-2.0，NOTICE 已署名）
- CI `build.yml` 版本号同步至 2.3.0 / v2.3.0

## [2.2.0] - 2026-09-03

### 修复

- **开关圆钮恒停左侧**（`GlassKit.GlassToggle`）：圆钮位移计算误用圆钮自身宽度（24dp）而非轨道宽度（52dp），导致位移恒为 -4dp；改为 `3dp + 22dp × progress`（两侧各留 3dp），圆钮现随开关状态正常滑动
- **底栏图标消失 / 幽灵槽 / 白团**（`AppRoot tabIcon`）：官方 `LiquidBottomTabs` 约定 `tabIcon(-1)` 渲染整排图标位，此前 `-1` 误入 when-else 分支只渲染单个全宽槽——导致底层玻璃胶囊仅显示居中幽灵「☰」、隐藏染色层无图标可折射（选中块白色圆团根因）。修复为 `-1` 时 `repeat(5)` 渲染全部 `TabIconSlot`

### 新增

- **题库版本机制**：`Repo.ensureBankLoaded` 由「仅 count==0 导入」升级为 DataStore `bank_version` 与 assets `version` 比对；不一致时清空学习数据后重导入（`ExamDao` 补 `clearExams` / `clearExamAnswers`）；`MainActivity` 以 `flow.first()` 取持久化值，避免组合态初值误触发
- 接入正式题库《无人机装调题库（含解析）》**800 题**（单选 640 / 判断 160，CSV 无分类列，统一命名「无人机装调」），JSON `version = 2`

### 变更

- ⚠️ **升级到本版本会自动重置学习记录**（题目 id 变化，旧记录必然失配，属预期行为）

### 工程

- `.gitignore` 补 `app/build/`，清理误跟踪的构建产物
- CI `build.yml` 版本号同步至 2.2.0 / v2.2.0

## [2.1.0] - 2026-09-03

### 修复

- **根治首次启动 SIGSEGV 闪退**（根因定位：官方 issue #54）——`layerBackdrop` 记录层内部不得存在采样同一 backdrop 的 `drawBackdrop` 节点（循环引用 → RenderThread native 崩溃，Java 层无法捕获）。v2.0 将 `NavHost` 内容层整体 `layerBackdrop` 且页面内玻璃组件全部采样，违反该硬约束
- 架构按官方语义重构（非降级）：
  - 新增 `Modifier.glassMaterial()`：iOS regular material 观感（半透明渐变表面 + 顶部高光描边 + 轻阴影），无采样循环风险
  - `GlassCard` / `GlassButton` / `GlassSlider` / `GlassToggle` 增加 `refracts` 参数：默认 `false`（内容流 = 材质）；`true` 且 `GlassRuntime.enabled` 时才走真折射（仅限记录层外）
  - `GlassBottomTabs`（记录层外的浮动元素）保持真折射玻璃不变
- **模考时长滑杆整体偏下**（`GlassSlider` 重写）：轨道与滑块置于同一 24dp 居中容器
- `GlassButton` 材质版增加按压缩放 + 高光描边；开关轨道样式重做

## [2.0.2] - 2026-09-03

### 新增

- **BootGuard 启动守护**：面包屑日志（`boot_log.txt`，150 行）+ 启动心跳（`boot_state.txt`，首帧渲染稳定 700ms 后 `markHealthy`，`onStop` 兜底）+ 连续异常死亡计数；兼容 native 崩溃 / 系统杀进程，无需 Java 堆栈即可定位死亡时点
- **自愈机制**：上次启动未标记健康即死 → 自动安全模式（`GlassRuntime.enabled = false`，全部玻璃组件退化为纯 Compose 渲染）；连续 2 次 → 启动弹出诊断屏（设备信息 + 版本 + 面包屑 + 一键复制）
- 设置页新增「画面特效」开关（DataStore `glass_effects` 持久化）；`MainActivity` 增加 `SafeModeBanner`

### 修复

- `CrashGuard` 升级：崩溃时同步写面包屑；报告屏合并启动轨迹

### 变更

- **固定签名**：生成 `dronequiz.keystore`（RSA 2048 / 30 年 / alias `dronequiz`），base64 加密上传 GitHub Secrets（`DQ_KEYSTORE_B64` / `DQ_KS_STORE_PASS`）；`build.gradle.kts` debug / release 统一读环境变量 `DQ_KS_PATH` / `DQ_KS_STORE_PASS`，本地与 CI 共用同一签名——**此后版本可覆盖安装，无需卸载旧版**

## [2.0.1] - 2026-09-03

### 修复

- 首帧绘制期 AGSL `RuntimeShader` 构造失败导致闪退的风险（部分 GPU 驱动对着色器语法兼容性差）：`lens()` / `HighlightStyle` / `InteractiveHighlight` 三处着色器构造全部加 `runCatching` 降级——失败时保留纯模糊 / 普通描边高光，不崩溃
- 数据库文件名改为 `drone_quiz_v2.db`，规避旧版残留库的 schema 校验冲突

### 新增

- `CrashGuard` 全局未捕获异常处理器：堆栈写入 `last_crash.txt`，下次启动在「启动报告」屏完整展示（可复制）
- `MainActivity` 增加可见的「正在加载题库…」启动画面（此前 ready 前为纯黑屏）

## [2.0.0] - 2026-09-03

首个正式版本，吸收两轮内测反馈后全量重建。

### 新增

- **数据层**：Room 七表（questions / practice_records / question_stats / exam_records / exam_answers / wrongbook / streak_log）+ DataStore 设置 + Repo
- **真液态玻璃 UI**：Kyant0 backdrop 库源码 vendor 进工程（Apache-2.0）；`GlassKit`（`glass()` 修饰符 = vibrancy + blur + AGSL lens 折射/色散）、`GlassButton`、`GlassSlider`（官方 DampedDrag 移植）、`GlassToggle`、`GlassCard`、`GlassBottomTabs`（官方移植，支持拖动切换）
- **五大页面**：首页（进度环 / 预估通过率 / 7 日柱状图 / 正确率折线 / 打卡 / 错题提示 / 上次模考）、刷题（筛选 / Pager / 点选即判 / 题号面板）、模考（全参数可配置 / 倒计时自动交卷）、错题本（连对移除 / 特训）、设置（主题 / 字号 / 提醒 / SAF 题库导入 / 清空记录）
- **动画系统**：NavHost 统一转场（淡入 + 轻缩放 280ms）、BounceState 过冲回弹、按压缩放、答错抖动、解析 spring 展开、倒计时红色脉冲
- **配色**：奶油底 + 墨黑主操作 + 橙色点缀，暖夜深色主题；底栏纯图标化
- **内置题库**：`gen_bank.py` 生成 801 题（单选 637 / 判断 164，11 分类，含解析）
- **CI/CD**：GitHub Actions `build.yml` 自动构建并发布 Release

### 修复（相对内测版）

- 模考交卷崩溃（`SQLiteConstraintException`）：`WrongBookEntity.addedAt` 改为非空默认 0，所有插入路径显式赋值时间戳；`fallbackToDestructiveMigration` 防旧库迁移崩溃
- 转场动画重叠、滑杆不可拖、列表末项被底栏遮挡（contentPadding 130dp）等十余项体验问题
- 首轮 GitHub Actions 31 个编译错误全部修复（context-parameters 编译器参数、`collectAsState` initial、`awaitFrame` → `withFrameNanos`、Lens 解耦 shapes 等）

[Unreleased]: https://github.com/Everett406/DroneQuiz/compare/v2.2.0...HEAD
[2.2.0]: https://github.com/Everett406/DroneQuiz/compare/v2.1.0...v2.2.0
[2.1.0]: https://github.com/Everett406/DroneQuiz/compare/v2.0.2...v2.1.0
[2.0.2]: https://github.com/Everett406/DroneQuiz/compare/v2.0.1...v2.0.2
[2.0.1]: https://github.com/Everett406/DroneQuiz/compare/v2.0.0...v2.0.1
[2.0.0]: https://github.com/Everett406/DroneQuiz/releases/tag/v2.0
